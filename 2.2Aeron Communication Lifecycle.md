---


---

<h1 id="aeron-communication-lifecycle">2.2Aeron Communication Lifecycle</h1>
<blockquote>
<p><strong>Aeron 发送生命周期 = 建通道 → 激活 → 稳态流 → 关闭</strong></p>
</blockquote>
<hr>
<h2 id="️⃣-建通道（控制路径，低频）">1️⃣ 建通道（控制路径，低频）</h2>
<pre><code>app  -&gt; addPublication()  -&gt; Client Conductor  -&gt; Command RingBuffer  -&gt; Driver Conductor  -&gt; 创建 Publication / LogBuffer / session / endpoint
</code></pre>
<p>👉 结果：</p>
<pre><code>Publication 可用，但还不能发数据（可能未连接）
</code></pre>
<hr>
<h2 id="️⃣-激活（连接建立）">2️⃣ 激活（连接建立）</h2>
<pre><code>Driver  ↔ 网络（Setup / Status Message）  → 建立 receiver / image  → subscriber ready
</code></pre>
<p>👉 关键：</p>
<pre><code>isConnected = true
</code></pre>
<hr>
<h2 id="️⃣-稳态流（数据路径，高频）">3️⃣ 稳态流（数据路径，高频）</h2>
<pre><code>Producer:  Publication.offer()    -&gt; 写 LogBufferDriver Sender:  -&gt; 读 LogBuffer  -&gt; 发 networkDriver Receiver:  -&gt; 收数据  -&gt; 写 Image LogBufferConsumer:  Subscription.poll()    -&gt; 读数据    -&gt; handler
</code></pre>
<hr>
<h2 id="️⃣-反馈调节（一直存在）">4️⃣ 反馈调节（一直存在）</h2>
<pre><code>Receiver -&gt; Status Message（消费进度）Receiver -&gt; NAK（丢包）→ Driver 调整发送窗口→ 影响 offer()（BACK_PRESSURED）
</code></pre>
<hr>
<h2 id="️⃣-关闭（控制路径）">5️⃣ 关闭（控制路径）</h2>
<pre><code>app -&gt; close Publication  -&gt; Client Conductor  -&gt; Driver Conductor  -&gt; 释放资源
</code></pre>
<hr>
<h1 id="最关键的理解（通透点）">最关键的理解（通透点）</h1>
<pre><code>控制路径：只在“建 / 改 / 关”时参与数据路径：一旦建立，就持续高速运行
</code></pre>
<hr>
<h1 id="一句话总结">一句话总结</h1>
<blockquote>
<p>Aeron 的生命周期是先通过控制路径建立通信结构，再进入独立的数据流阶段，期间通过协议反馈持续调节，最终再通过控制路径释放资源。</p>
</blockquote>

