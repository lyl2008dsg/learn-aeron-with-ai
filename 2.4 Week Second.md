## 第 2 周：先看 client 侧源码

建议顺序：

1.  `Aeron`
2.  `Context`
3.  `Publication`
4.  `ConcurrentPublication`
5.  `ExclusivePublication`
6.  `Subscription`
7.  `Image`
8.  `FragmentAssembler`

这里重点理解：

-   client 初始化时做了什么
-   addPublication / addSubscription 到底干了什么
-   `offer()` 失败时几种返回码各代表什么
-   `poll()` / `controlledPoll()` 的区别
-   `Image` 为什么是订阅数据的实际承载对象

### 这一阶段你要画出两张图

第一张：**发布链路**  
`app -> Publication.offer -> client conductor -> driver -> sender -> network`

第二张：**订阅链路**  
`network -> receiver -> driver -> image/log buffer -> Subscription.poll -> handler`



--------

## 1. client 初始化做了什么？

> Aeron.connect() = 建立 client 与 driver 的连接

流程：

Context → 初始化配置  
→ 创建 Client Conductor  
→ 建立 Command / Response RingBuffer  
→ 启动 conductor（线程或 invoker）  
→ 开始 CLIENT_KEEPALIVE  

一句话：
client 初始化就是“把控制通道建起来”

---

## 2. addPublication / addSubscription 做了什么？

> 本质：发命令，让 Driver 创建资源

流程：

app  
→ Client Conductor  
→ Command RingBuffer（ADD_PUBLICATION / ADD_SUBSCRIPTION）  
→ Driver Conductor  
→ 创建 LogBuffer / session / image  

一句话：
addXxx = “声明我要一条流”，真正资源在 Driver 创建

---

## 3. offer() 失败返回码

> offer() ≠ 一定成功

常见返回：
```
BACK_PRESSURED         → 接收端太慢  
NOT_CONNECTED          → 还没建立连接  
ADMIN_ACTION           → Driver 正在调整  
CLOSED                 → 已关闭  
MAX_POSITION_EXCEEDED  → 写到上限  
```
一句话：
offer 失败本质是“当前不能继续写”

---

## 4. poll() vs controlledPoll()

> 都是消费，但控制能力不同

poll()：
- 批量消费
- 一次性处理

controlledPoll()：
- 每条可控
- 可 break / abort / commit
- controlledPoll 通过“是否推进 position”实现可控消费（精细提交 / 回滚）。

一句话：
* poll = 批处理  
* controlledPoll = 精细控制


为什么 controlledPoll 能“回滚”？
* 其实是“没推进 position”，不是 undo

---

## 5. Image 为什么是数据承载？

**Image 数据结构**

>**Image = 一个 session 的接收视图（Subscriber 侧的数据入口)**

### 核心组成

```text
Image
 ├─ sessionId / streamId
 ├─ LogBuffer（3 × term buffer + metadata）
 ├─ subscriberPosition（当前消费进度）
 ├─ initialTermId / termLength（位置计算基准）
 └─ flags（isClosed / isEndOfStream）
```

关系：

Subscription = 多个 Image  
Image = 一个 publisher（sessionId）  

作用：
- 持有 LogBuffer
- 管理 position
- 提供 poll 数据

一句话：
Image 才是真正“拿数据”的地方

---

## 6. 两种 Publication

ConcurrentPublication：
- 多线程安全
- 有额外开销

ExclusivePublication：
- 单线程
- 更快

一句话：
单线程用 Exclusive，性能更好

---

## 7. FragmentAssembler 是什么？

> 解决消息分片问题

作用：

- 把多个 fragment 拼成完整 message

一句话：
Assembler = 帮你还原大消息

---

## 本周必须画两张图

---

### 1️⃣ 发布链路（修正版）

app  
→ Publication.offer()  
→ 写 LogBuffer  
→ Driver Sender  
→ Network  

一句话：
offer 不发网络，只写 buffer

---

### 2️⃣ 订阅链路

Network  
→ Driver Receiver  
→ Image LogBuffer  
→ Subscription.poll()  
→ handler  

一句话：
* 数据最终从 Image 被读出

---

## 本周目标

你要能讲清：

1. Aeron.connect 做了什么  
2. addPublication / addSubscription 的本质  
3. offer 为什么会失败  
4. poll vs controlledPoll 区别  
5. Image 为什么存在  
6. 数据链路（发布 / 订阅）

---

> 第二周目标：搞清 client API 背后到底在驱动什么
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTc2NTM3MzA3N119
-->