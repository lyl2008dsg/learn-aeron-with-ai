---


---

<h1 id="七、第四阶段：最后读-cluster">七、第四阶段：最后读 Cluster</h1>
<blockquote>
<p>核心目标：理解 Aeron Cluster 如何实现“有状态 + 一致性 + 可恢复”的分布式系统</p>
</blockquote>
<p>Cluster 是 Aeron 里最复杂的一块。官方 wiki 直接说它“moving parts 很多”，需要多个组件协调；并提供了 Cluster Component Model 和 Cluster Tutorial。</p>
<p>所以不要太早看。<br>
等你把 Core + Archive 基本打透，再进 Cluster。</p>
<h3 id="cluster-阅读顺序">Cluster 阅读顺序</h3>
<ol>
<li><strong>Cluster Component Model</strong></li>
<li><strong>Cluster Tutorial</strong></li>
<li>源码再看：
<ul>
<li><code>ConsensusModule</code></li>
<li><code>ClusteredServiceContainer</code></li>
<li><code>AeronCluster</code></li>
<li><code>Election</code></li>
<li><code>LogPublisher</code></li>
<li><code>IngressAdapter / EgressAdapter</code></li>
</ul>
</li>
</ol>
<h3 id="你要先理解的不是代码，而是角色">你要先理解的不是代码，而是角色</h3>
<ul>
<li>client 怎么进 cluster</li>
<li>leader 如何接收 ingress</li>
<li>log 如何复制</li>
<li>follower 如何追平</li>
<li>snapshot 在哪里生成和恢复</li>
<li>service container 为什么和 consensus module 分离</li>
</ul>
<h2 id="第-7-周：最后读-cluster">第 7 周：最后读 Cluster</h2>
<h3 id="一句话模型">一句话模型</h3>
<blockquote>
<p>Cluster = 共识（Consensus）+ 日志复制（Log）+ 状态机（Service）</p>
</blockquote>
<hr>
<h2 id="先理解角色（比源码更重要）">先理解角色（比源码更重要）</h2>
<h3 id="️⃣-client-怎么进-cluster？">1️⃣ client 怎么进 cluster？</h3>
<pre><code>client
→ AeronCluster（客户端库）
→ ingress（发送请求）
→ leader
</code></pre>
<p>一句话：</p>
<blockquote>
<p>client 只和 leader 交互</p>
</blockquote>
<hr>
<h3 id="️⃣-leader-如何接收-ingress？">2️⃣ leader 如何接收 ingress？</h3>
<pre><code>IngressAdapter
→ 接收 client 请求
→ 写入 log（LogPublisher）
→ 提交到 consensus
</code></pre>
<p>一句话：</p>
<blockquote>
<p>leader 把所有请求先写入 log</p>
</blockquote>
<hr>
<h3 id="️⃣-log-如何复制？">3️⃣ log 如何复制？</h3>
<pre><code>leader
→ LogPublisher
→ 通过 Aeron stream
→ followers 接收
</code></pre>
<p>一句话：</p>
<blockquote>
<p>log = cluster 的“真相来源”（source of truth）</p>
</blockquote>
<hr>
<h3 id="️⃣-follower-如何追平？">4️⃣ follower 如何追平？</h3>
<pre><code>follower
→ 接收 log stream
→ 写本地 log
→ 按顺序执行
</code></pre>
<p>一句话：</p>
<blockquote>
<p>follower = 被动复制 + 顺序执行</p>
</blockquote>
<hr>
<h3 id="️⃣-snapshot-在哪里生成？">5️⃣ snapshot 在哪里生成？</h3>
<pre><code>ClusteredServiceContainer
→ 定期生成 snapshot
→ 保存 service 状态
</code></pre>
<p>恢复：</p>
<pre><code>启动
→ 加载 snapshot
→ replay log（未覆盖部分）
</code></pre>
<p>一句话：</p>
<blockquote>
<p>snapshot + log = 完整恢复状态</p>
</blockquote>
<hr>
<h3 id="️⃣-为什么-service-和-consensus-分离？">6️⃣ 为什么 service 和 consensus 分离？</h3>
<pre><code>ConsensusModule：
  负责一致性（leader / election / log）

ServiceContainer：
  负责业务逻辑（状态机）
</code></pre>
<p>👉 好处：</p>
<pre><code>解耦
容错
可替换业务
</code></pre>
<p>一句话：</p>
<blockquote>
<p>共识和业务分离，降低复杂度</p>
</blockquote>
<hr>
<h2 id="核心组件（源码对应）">核心组件（源码对应）</h2>
<pre><code>ConsensusModule           → 共识核心
ClusteredServiceContainer → 状态机执行
AeronCluster              → 客户端入口
Election                  → 选主流程
LogPublisher              → 写 log
IngressAdapter            → 收请求
EgressAdapter             → 回响应
</code></pre>
<hr>
<h2 id="数据流（必须理解）">数据流（必须理解）</h2>
<hr>
<h3 id="请求流程（ingress）">请求流程（Ingress）</h3>
<pre><code>client
→ ingress
→ leader
→ 写 log
→ 复制到 followers
→ commit
→ service 执行
→ egress 返回结果
</code></pre>
<hr>
<h3 id="恢复流程（recovery）">恢复流程（Recovery）</h3>
<pre><code>加载 snapshot
→ replay log
→ 恢复状态
→ 继续处理请求
</code></pre>
<hr>
<h2 id="本周三条主线">本周三条主线</h2>
<pre><code>请求：client → leader → log → service
复制：leader → followers（log replication）
恢复：snapshot + replay
</code></pre>
<hr>
<h2 id="本周目标">本周目标</h2>
<p>你要能讲清：</p>
<ol>
<li>client 如何进入 cluster</li>
<li>leader 如何处理请求（ingress）</li>
<li>log 如何复制到 follower</li>
<li>follower 如何追平</li>
<li>snapshot 如何生成和恢复</li>
<li>为什么 service 与 consensus 分离</li>
</ol>
<hr>
<blockquote>
<p>第七周目标：搞清 Aeron 如何实现“分布式一致性 + 状态机执行”</p>
</blockquote>

