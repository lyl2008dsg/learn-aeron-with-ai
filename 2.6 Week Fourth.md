---


---

<h2 id="第-4-周：专项读“数据结构”">第 4 周：专项读“数据结构”</h2>
<blockquote>
<p>核心目标：搞清 Aeron 的底层数据结构是如何支撑性能与协议的<br>
这是 Aeron 真正拉开差距的地方。</p>
</blockquote>
<p>重点盯这些概念：</p>
<ul>
<li><code>LogBuffer</code></li>
<li><code>TermBuffer</code></li>
<li><code>FrameDescriptor</code></li>
<li><code>DataHeaderFlyweight</code></li>
<li><code>SetupFlyweight</code></li>
<li><code>StatusMessageFlyweight</code></li>
<li><code>NakFlyweight</code></li>
<li><code>Position</code></li>
<li><code>Counters</code></li>
</ul>
<p>官方 wiki 有单独的 Transport Protocol Specification、Data Structures、Configuration、Monitoring 文档，正对应这些内容。</p>
<h3 id="你必须真正搞懂的几个问题">你必须真正搞懂的几个问题</h3>
<ul>
<li>为什么是 <strong>term</strong> 的概念，而不是无限 buffer？</li>
<li>为什么 term rotation 能减少复杂度？</li>
<li>position 和 term offset、term id 如何互转？</li>
<li>为什么很多地方用 flyweight？</li>
<li>为什么 counters 对排障这么重要？</li>
</ul>
<h2 id="一句话模型">一句话模型</h2>
<blockquote>
<p>Aeron = 基于 LogBuffer（分段 term） + position 推进 的顺序写数据结构</p>
</blockquote>
<hr>
<h2 id="本周必须搞懂五个问题">本周必须搞懂五个问题</h2>
<h3 id="️⃣-为什么是-term，而不是无限-buffer？">1️⃣ 为什么是 term，而不是无限 buffer？</h3>
<blockquote>
<p>无限 buffer 不可控，term 是“可循环的固定分段”</p>
</blockquote>
<pre><code>无限 buffer：
  内存不可控 + 回收复杂 + 延迟不可预测

term（固定大小）：
  顺序写 + 可复用 + 有边界
</code></pre>
<p>一句话：</p>
<blockquote>
<p><strong>term = 可控的循环缓冲单元</strong></p>
</blockquote>
<hr>
<h3 id="️⃣-为什么-term-rotation-能降低复杂度？">2️⃣ 为什么 term rotation 能降低复杂度？</h3>
<blockquote>
<p>写满就切，避免复杂内存管理</p>
</blockquote>
<pre><code>term0 → term1 → term2 → term0（复用）
</code></pre>
<p>避免：</p>
<pre><code>❌ 动态扩容
❌ 数据搬迁
❌ 复杂回收
</code></pre>
<p>一句话：</p>
<blockquote>
<p><strong>rotation 用“切换”代替“管理”</strong></p>
</blockquote>
<hr>
<h3 id="️⃣-position--termid--termoffset-怎么互转？">3️⃣ position / termId / termOffset 怎么互转？</h3>
<blockquote>
<p>position = 全局进度，term 是局部分段</p>
</blockquote>
<pre><code>position = termId * termLength + termOffset
</code></pre>
<p>反推：</p>
<pre><code>termId     = position / termLength
termOffset = position % termLength
</code></pre>
<p>一句话：</p>
<blockquote>
<p><strong>position 是全局坐标，term 是分段坐标</strong></p>
</blockquote>
<hr>
<h3 id="️⃣-为什么用-flyweight？">4️⃣ 为什么用 Flyweight？</h3>
<blockquote>
<p>Flyweight = 直接在 buffer 上读写，避免对象创建</p>
</blockquote>
<pre><code>DataHeaderFlyweight			应数据
SetupFlyweight					建链
StatusMessageFlyweight	流控
NakFlyweight						重传
</code></pre>
<p>特点：</p>
<pre><code>不 new 对象
直接 wrap buffer
按 offset 读字段
</code></pre>
<p>一句话：</p>
<blockquote>
<p><strong>Flyweight 用“内存视图”代替“对象”</strong></p>
</blockquote>
<p>采用 DirectBuffer实现：</p>
<p><strong>Agrona 的 DirectBuffer 是一个无状态、按 offset 访问的内存视图，比 ByteBuffer 更接近底层内存，从而支撑 Aeron 的高性能。</strong></p>
<p>DirectBuffer 核心模式：</p>
<ul>
<li>写（encode）</li>
<li>读（decode）</li>
<li>视图（wrap）</li>
</ul>
<p>DirectBuffer 核心用途：</p>
<ul>
<li>适合
<ul>
<li>低延迟（μs级）</li>
<li>高吞吐</li>
<li>对象分配敏感</li>
<li>需要零拷贝</li>
<li>协议明确（二进制结构固定）</li>
</ul>
</li>
<li>不适合
<ul>
<li>业务逻辑复杂</li>
<li>数据结构频繁变化</li>
<li>需要可读性（JSON / POJO）</li>
<li>普通 CRUD 系统</li>
</ul>
</li>
</ul>
<hr>
<h3 id="️⃣-为什么-counters-很重要？">5️⃣ 为什么 Counters 很重要？</h3>
<blockquote>
<p>Counters = Driver 的可观测状态</p>
</blockquote>
<pre><code>position
limit
back pressure 次数
NAK 次数
flow control 状态
</code></pre>
<p>作用：</p>
<pre><code>定位：
- 为什么发不出去？
- 谁慢？
- 哪个流卡住？
</code></pre>
<p>一句话：</p>
<blockquote>
<p><strong>Counters 是调试 Aeron 的唯一窗口</strong></p>
</blockquote>
<hr>
<h2 id="关键数据结构关系">关键数据结构关系</h2>
<blockquote>
<p>position 描述数据流推进，counters 描述系统状态，两者共同决定 Aeron 是否“正常流动”。<br>
数据在 LogBuffer 里流，状态在 Counters 里体现。</p>
</blockquote>
<pre><code>LogBuffer
 ├─ TermBuffer × 3
 │    ├─ data region（frame）
 │    └─ metadata
 └─ counters / position
</code></pre>
<pre><code>LogBuffer
 ├─ TermBuffer × 3
 │    ├─ data region（frame）
 │    └─ metadata（tail / termId / status）
 └─ MetaData（LogMetaData）
      ├─ initialTermId
      ├─ activeTermCount
      ├─ termLength
      ├─ mtuLength
      ├─ correlationId
      ├─ endOfStream / isConnected
      ├─ termTailCounters[3]
      └─ positionBitsToShift

Counters
 ├─ publisherPosition		→ 写到哪了（offer）
 ├─ senderPosition			→ 发到哪了
 ├─ receiverPosition		→ 收到哪了
 ├─ subscriberPosition	→ 消费到哪了
 ├─ senderLimit（flow control window）
 ├─ backPressureCount
 ├─ nakCount
 └─ statusMessageCount
</code></pre>
<hr>
<h2 id="协议对象（flyweight）">协议对象（Flyweight）</h2>
<pre><code>Data Frame           → DataHeaderFlyweight
连接建立             → SetupFlyweight
流控反馈             → StatusMessageFlyweight
丢包恢复             → NakFlyweight
</code></pre>
<hr>
<h2 id="本周三条主线">本周三条主线</h2>
<pre><code>结构：LogBuffer / TermBuffer（数据怎么存）
坐标：Position / termId / offset（怎么定位）
协议：Flyweight（怎么读写数据）
</code></pre>
<hr>
<h2 id="本周目标">本周目标</h2>
<p>你要能讲清：</p>
<ol>
<li>为什么要用 term 而不是无限 buffer</li>
<li>term rotation 如何降低复杂度</li>
<li>position / termId / offset 的关系</li>
<li>flyweight 的本质和作用</li>
<li>counters 在排障中的价值</li>
</ol>
<hr>
<blockquote>
<p>第四周目标：搞清 Aeron 底层“数据是如何表示和定位的”</p>
</blockquote>
<h2 id="进阶">进阶</h2>
<h3 id="为什么-agrona-还要做-boundscheck--alignment--endian-处理？">为什么 Agrona 还要做 BoundsCheck / alignment / endian 处理？</h3>
<p>一句话</p>
<blockquote>
<p><strong>BoundsCheck / alignment / endian = 在“接近裸内存”的前提下，保证安全、性能和跨平台一致性。</strong><br>
BoundsCheck 保安全，alignment 保性能，endian 保一致性。</p>
</blockquote>
<p>BoundsCheck</p>
<blockquote>
<p>是什么？<br>
确保 offset 不越界<br>
为什么需要？<br>
Unsafe 访问不会帮你检查  → 越界 = 内存破坏 / JVM 崩溃<br>
BoundsCheck = 用一点点开销换“不会炸进程”</p>
</blockquote>
<p>alignment（对齐）</p>
<blockquote>
<p>CPU 访问对齐数据更快，避免跨 cache line / 多次读写<br>
alignment = 让 CPU 以最高效率读取内存</p>
</blockquote>
<p>endian（字节序）</p>
<blockquote>
<p>多字节数据的存储顺序，保证不同机器之间读出来是同一个值。<br>
Big Endian    高位在前<br>
Little Endian 低位在前（主流 CPU）</p>
</blockquote>

