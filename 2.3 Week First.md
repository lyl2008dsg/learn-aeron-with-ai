# 第一阶段：先精通 Core，不碰 Cluster

## 第 1 周：建立全局心智模型
> Command RingBuffer 传“意图”，LogBuffer 传“数据”。
> app -> Publication.offer -> client conductor  
-> media driver -> sender -> network  
-> receiver -> image/log buffer -> subscription.poll -> handler

先读官方这些文档，再读源码：

1.  **Design Overview**
2.  **Data Structures**
3.  **Design Principles**
4.  **Media Driver Operation**
5.  **Transport Protocol Specification**

官方 wiki 正是把这些放在 “How does Aeron work?” 下，说明这是理解源码前的主干资料。

你这一周的目标不是记代码，而是回答这几个问题：

-   Aeron 为什么一定要有 **Media Driver**？
-   client 和 driver 之间怎么通信？
-   `channel + streamId + sessionId` 分别是什么语义？
-   `Publication` 为什么会 back pressure？
-   `Subscription.poll()` 为什么是 pull 而不是回调？
-   term buffer 为什么分区轮转？
-   position 是怎么推进的？


-----------------------------------


## 1. Aeron 为什么一定要有 Media Driver？

> Media Driver = 用户态消息内核

Data Path：
payload → LogBuffer → Sender → Network → Receiver → LogBuffer → poll

Control Path：
Publication / Subscription、session / stream、Flow Control、Back Pressure、NAK、Status

一句话：
Media Driver 把网络、buffer、流控和可靠性统一下沉到一个高性能执行引擎中

---

## 2. Client 和 Driver 怎么通信？

> Client ↔ Driver = 共享内存 + RingBuffer

Client Conductor → Command RingBuffer → Driver Conductor → Response RingBuffer → Client

一句话：
控制走 RingBuffer，数据走 LogBuffer，完全分离

---

## 3. channel / streamId / sessionId 语义

> 三者共同定位一条数据流

channel   = 传输路径（走哪条路）  
streamId  = 逻辑流（哪条流）  
sessionId = 发送者（谁发的）  

Receiver 处理顺序：
channel → streamId → sessionId

一句话：
channel 定位路径，streamId 分流，sessionId 区分发送者

---

## 4. Publication 为什么会 Back Pressure？

> 写得比消费快，被流控限制

offer → 写 LogBuffer  
→ Receiver 消费慢  
→ Status Message 不推进  
→ sender limit 达上限  
→ offer 返回 BACK_PRESSURED  

一句话：
Back Pressure = 接收端限制发送端

---

## 5. Subscription.poll() 为什么是 pull？

> pull = 应用掌控执行节奏

原因：
- 控制消费速率（batch）
- 避免回调带来的抖动
- 参与流控（消费慢 → 限流）
- 简化并发模型（event loop）

一句话：
pull 保证低延迟和可预测性

---

## 6. term buffer 为什么分区轮转？

> 分区轮转 = 持续顺序写 + 避免覆盖

流程：
当前 term 写满 → 切换 → 旧 term 等消费后复用

通常 3 个：
- active（写）
- in-use（读）
- next（备用）

一句话：
用最少分区实现无锁顺序写和读写隔离

---

## 7. position 怎么推进？

> position = 数据处理进度（字节级）

推进：
offer → producer position  
send → sender position  
receive → receiver position  
poll → subscriber position  

一句话：
谁处理完数据，谁推进 position

---

## 本周目标

你要能讲清：

1. Media Driver 的作用
2. 控制路径 vs 数据路径
3. 三元组如何定位数据流
4. Back Pressure 原理
5. poll 为什么是 pull
6. term buffer 轮转原因
7. position 表达什么

> 第一周目标：建立心智模型，不追源码细节
<!--stackedit_data:
eyJoaXN0b3J5IjpbMTI1MjQxNzUyNl19
-->