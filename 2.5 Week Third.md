---


---

<h2 id="第-3-周：重点啃-media-driver">第 3 周：重点啃 Media Driver</h2>
<p>源码核心在这里。你优先看这些角色：</p>
<ul>
<li><code>DriverConductor</code></li>
<li><code>Sender</code></li>
<li><code>Receiver</code></li>
</ul>
<p>Aeron 的 driver 运行本质上就是几个 agent 的 duty cycle。官方文档在 Media Driver Operation、Monitoring and Debugging、Design Types and Patterns 里都强调了 agent / counter / duty cycle 这类运行方式。</p>
<h3 id="这周只搞懂三件事">这周只搞懂三件事</h3>
<ol>
<li><strong>资源创建与生命周期</strong>
<ul>
<li>publication 怎么被创建</li>
<li>subscription 怎么关联</li>
<li>network publication / ipc publication 区别</li>
</ul>
</li>
<li><strong>发送路径</strong>
<ul>
<li>buffer claim / append</li>
<li>sender 怎么扫描可发送的数据</li>
<li>flow control 怎么限制发送窗口</li>
</ul>
</li>
<li><strong>接收路径</strong>
<ul>
<li>receiver 收到 data frame 后如何写入 term buffer</li>
<li>loss detection / gap detection</li>
<li>NAK / Status Message 怎么参与恢复</li>
</ul>
</li>
</ol>
<hr>
<h3 id="一句话模型">一句话模型</h3>
<blockquote>
<p>Driver = 多个 Agent 循环执行 doWork（duty cycle）</p>
</blockquote>
<pre class=" language-text"><code class="prism  language-text">while (running) {
  work = doWork()
  idleStrategy.idle(work)
}
</code></pre>
<h2 id="本周只搞三件事">本周只搞三件事</h2>
<h3 id="️⃣-资源创建与生命周期">1️⃣ 资源创建与生命周期</h3>
<blockquote>
<p>本质：DriverConductor 处理命令 → 创建资源</p>
</blockquote>
<p>流程：</p>
<pre class=" language-text"><code class="prism  language-text">Client
→ Command RingBuffer
→ DriverConductor
→ 创建 Publication / Subscription / LogBuffer / Image
</code></pre>
<h4 id="publication-怎么创建？">publication 怎么创建？</h4>
<pre class=" language-text"><code class="prism  language-text">ADD_PUBLICATION
→ DriverConductor
→ 创建 NetworkPublication / IpcPublication
→ 分配 LogBuffer
→ 注册到 Sender
</code></pre>
<h4 id="subscription-怎么关联？">subscription 怎么关联？</h4>
<pre class=" language-text"><code class="prism  language-text">ADD_SUBSCRIPTION
→ DriverConductor
→ 创建 SubscriptionLink
→ Receiver 收到数据
→ 匹配 streamId / sessionId
→ 创建 Image
→ 绑定到 Subscription
</code></pre>
<h4 id="network-vs-ipc-publication">network vs ipc publication</h4>
<pre class=" language-text"><code class="prism  language-text">NetworkPublication：走 UDP（跨进程 / 跨机器）
IpcPublication：走共享内存（同机）
</code></pre>
<p>一句话：network 走网络，ipc 走内存</p>
<hr>
<h3 id="️⃣-发送路径（sender）">2️⃣ 发送路径（Sender）</h3>
<blockquote>
<p>本质：从 LogBuffer 找数据 → 发出去</p>
</blockquote>
<h4 id="写数据（producer）">写数据（Producer）</h4>
<pre class=" language-text"><code class="prism  language-text">offer() → append（写 term buffer）
bufferClaim() → 零拷贝写入
</code></pre>
<h4 id="sender-怎么发？">Sender 怎么发？</h4>
<pre class=" language-text"><code class="prism  language-text">Sender.doWork()
→ 扫描所有 publication
→ 找 position &lt; limit 的数据
→ 按 MTU 切片
→ 发送
</code></pre>
<h4 id="flow-control-怎么限制？">flow control 怎么限制？</h4>
<pre class=" language-text"><code class="prism  language-text">Receiver → Status Message（消费进度）
→ Driver 计算 sender limit
→ Sender 不能发超过 limit
→ offer() 可能 BACK_PRESSURED
</code></pre>
<p>一句话：Receiver 控制 Sender 发多少</p>
<hr>
<h3 id="️⃣-接收路径（receiver）">3️⃣ 接收路径（Receiver）</h3>
<blockquote>
<p>本质：收包 → 写 LogBuffer → 修复丢包</p>
</blockquote>
<h4 id="收到数据后">收到数据后</h4>
<pre class=" language-text"><code class="prism  language-text">UDP packet
→ 解析 header（termId / offset）
→ 写入 Image LogBuffer
→ 更新 position
</code></pre>
<h4 id="丢包检测（gap-detection）">丢包检测（gap detection）</h4>
<pre class=" language-text"><code class="prism  language-text">发现 offset 不连续
→ 标记 gap
</code></pre>
<h4 id="恢复机制">恢复机制</h4>
<pre class=" language-text"><code class="prism  language-text">Receiver → NAK（请求重传）
Sender → 重发缺失数据
Receiver → Status Message（窗口 + 进度）
</code></pre>
<hr>
<h2 id="本周三条主线">本周三条主线</h2>
<pre class=" language-text"><code class="prism  language-text">资源：DriverConductor 创建和管理资源
发送：Producer 写 → Sender 发 → Flow Control 限制
接收：Receiver 收 → 写 buffer → 检测并恢复
</code></pre>
<hr>
<h2 id="本周目标">本周目标</h2>
<p>你要能讲清：</p>
<ol>
<li>Driver 是怎么循环执行的（duty cycle）</li>
<li>publication / subscription 怎么创建和关联</li>
<li>Sender 怎么找到数据并发送</li>
<li>Flow Control 如何限制发送</li>
<li>Receiver 怎么写数据 + 检测丢包 + 恢复</li>
</ol>
<hr>
<blockquote>
<p>第三周目标：搞清 Driver 内部“数据是怎么真正流动的”</p>
</blockquote>
<blockquote>
<ol>
<li><strong>Driver 通过不断轮询（duty cycle）执行发送、接收和控制逻辑，从而实现低延迟和无锁高吞吐。</strong></li>
<li></li>
</ol>
</blockquote>

