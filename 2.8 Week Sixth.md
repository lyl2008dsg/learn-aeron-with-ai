---


---

<h1 id="六、第三阶段：再读-archive">六、第三阶段：再读 Archive</h1>
<blockquote>
<p>核心目标：理解 Aeron 如何实现“可回溯的数据流”（record + replay）</p>
</blockquote>
<p>Archive 官方定义很明确：它是一个高性能的 record/replay 服务，性能瓶颈通常主要受存储影响，足够快的存储下可达到很高吞吐。</p>
<p>所以 Archive 你要从“它解决什么问题”入手：</p>
<ul>
<li>为什么需要 archive，而不是业务自己落盘？</li>
<li>recording 和 replay 的边界在哪？</li>
<li>control session 是怎么控制 record/replay 的？</li>
<li>catalog 记录了什么？</li>
<li>replay merge 是怎么服务追平的？</li>
</ul>
<h3 id="源码顺序建议">源码顺序建议</h3>
<ol>
<li><code>Archive</code></li>
<li><code>Archive.Context</code></li>
<li><code>ArchiveConductor</code></li>
<li><code>RecordingSession</code></li>
<li><code>ReplaySession</code></li>
<li><code>Catalog</code></li>
</ol>
<h3 id="学习目标">学习目标</h3>
<p>你能说清楚：</p>
<ul>
<li>record 是怎么把 stream 持久化的</li>
<li>replay 是怎么重新喂给订阅者的</li>
<li>catalog 如何管理 recording metadata</li>
</ul>
<h3 id="一句话模型">一句话模型</h3>
<blockquote>
<p>Archive = 把流数据录下来（record），再按需重放（replay）</p>
</blockquote>
<hr>
<h2 id="先回答三个核心问题">先回答三个核心问题</h2>
<hr>
<h3 id="️⃣-为什么需要-archive？">1️⃣ 为什么需要 Archive？</h3>
<blockquote>
<p>业务自己落盘 ≠ 高性能流式持久化</p>
</blockquote>
<pre><code>业务落盘：
  ❌ 侵入业务逻辑
  ❌ 复制数据（额外开销）
  ❌ 不统一（难 replay）

Archive：
  ✔ 直接订阅 stream
  ✔ 零拷贝写文件
  ✔ 原生支持 replay
</code></pre>
<p>一句话：</p>
<blockquote>
<p>Archive 是“旁路录制”，不影响主链路</p>
</blockquote>
<hr>
<h3 id="️⃣-recording-vs-replay">2️⃣ recording vs replay</h3>
<pre><code>recording：
  从 live stream → 写入文件

replay：
  从文件 → 重新发布成 stream
</code></pre>
<p>一句话：</p>
<blockquote>
<p>record 负责“存”，replay 负责“再发一遍”</p>
</blockquote>
<hr>
<h3 id="️⃣-control-session-是什么？">3️⃣ control session 是什么？</h3>
<blockquote>
<p>控制平面（不是数据）</p>
</blockquote>
<pre><code>client → Archive（control channel）
→ startRecording
→ stopRecording
→ startReplay
→ stopReplay
</code></pre>
<p>一句话：</p>
<blockquote>
<p>control session = 控制 Archive 行为的 RPC 通道</p>
</blockquote>
<hr>
<h2 id="核心组件">核心组件</h2>
<h3 id="️⃣-archive--context">1️⃣ Archive / Context</h3>
<pre><code>Archive 启动入口
Context 提供配置（目录 / channel / buffer 等）
</code></pre>
<hr>
<h3 id="️⃣-archiveconductor">2️⃣ ArchiveConductor</h3>
<blockquote>
<p>核心调度器（类似 DriverConductor）</p>
</blockquote>
<pre><code>处理控制命令
创建 RecordingSession / ReplaySession
管理生命周期
</code></pre>
<hr>
<h3 id="️⃣-recordingsession">3️⃣ RecordingSession</h3>
<blockquote>
<p>负责 record</p>
</blockquote>
<pre><code>订阅指定 stream
→ 从 Image 读取数据
→ 写入 recording file（磁盘）
→ 更新 position
</code></pre>
<p>一句话：</p>
<blockquote>
<p>RecordingSession = “把流写到文件”</p>
</blockquote>
<hr>
<h3 id="️⃣-replaysession">4️⃣ ReplaySession</h3>
<blockquote>
<p>负责 replay</p>
</blockquote>
<pre><code>从 recording file 读取
→ 按 offset 读取数据
→ 通过 Publication 发出去
</code></pre>
<p>一句话：</p>
<blockquote>
<p>ReplaySession = “把文件变回流”</p>
</blockquote>
<hr>
<h3 id="️⃣-catalog">5️⃣ Catalog</h3>
<blockquote>
<p>元数据管理（核心）</p>
</blockquote>
<p>记录：</p>
<pre><code>recordingId
channel / streamId
startPosition / stopPosition
termLength
segmentFile
状态（active / stopped）
</code></pre>
<p>一句话：</p>
<blockquote>
<p>Catalog = 所有 recording 的“索引表”</p>
</blockquote>
<hr>
<h2 id="数据流（必须理解）">数据流（必须理解）</h2>
<hr>
<h3 id="record-流程">record 流程</h3>
<pre><code>live stream
→ Archive 订阅（Image）
→ RecordingSession.poll()
→ 写入文件（segment）
→ 更新 Catalog
</code></pre>
<hr>
<h3 id="replay-流程">replay 流程</h3>
<pre><code>client 请求 replay
→ ReplaySession
→ 从文件读取数据
→ Publication.offer()
→ 下游订阅者 poll()
</code></pre>
<hr>
<h2 id="replay-merge（追平-live）">replay merge（追平 live）</h2>
<blockquote>
<p>关键能力：先 replay，再切 live</p>
</blockquote>
<pre><code>历史数据 → replay
接近实时 → merge
最终切换 → live stream
</code></pre>
<p>一句话：</p>
<blockquote>
<p>replay merge = “先追历史，再接实时”</p>
</blockquote>
<hr>
<h2 id="本周三条主线">本周三条主线</h2>
<pre><code>record：流 → 文件
replay：文件 → 流
catalog：管理 recording 元信息
</code></pre>
<hr>
<h2 id="本周目标">本周目标</h2>
<p>你要能讲清：</p>
<ol>
<li>为什么需要 Archive（vs 自己落盘）</li>
<li>record 是怎么写入文件的</li>
<li>replay 是怎么重新发布数据的</li>
<li>control session 如何控制 Archive</li>
<li>catalog 记录了哪些元信息</li>
<li>replay merge 如何实现追平</li>
</ol>
<hr>
<blockquote>
<p>第六周目标：搞清 Aeron 如何实现“可回放的数据流系统”</p>
</blockquote>

